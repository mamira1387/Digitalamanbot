# از یک ایمیج پایه PHP استفاده کنید
FROM php:8.2-apache

# فایل‌های پروژه رو به دایرکتوری وب سرور کپی کنید
COPY . /var/www/html/

# مطمئن بشید که Apache از دایرکتوری /var/www/html استفاده میکنه
# (معمولا در ایمیج های PHP-Apache پیش‌فرض هست)

# پورت 80 رو برای دسترسی وب اکسپوز کنید
EXPOSE 80

# Apache به صورت خودکار با ایمیج پایه اجرا میشه
# نیازی به CMD یا ENTRYPOINT اضافی نیست مگر اینکه تنظیمات خاصی داشته باشید
